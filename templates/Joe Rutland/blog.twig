{{ include('./sections/header.twig') }}
<div class="blog-container">
    <h1>{{page.title}}</h1>
    <h2>{{page.date}}</h2>
    
    <div class="tags">
    {% for tag in page.tags %}
        <span>{{tag}}</span>
    {% endfor %}
    </div>

    <div class="markdown-content">{{page.markdown|markdown}}</div>
</div>
{{ include('./sections/footer.twig') }}