<section id="{{section.name}}" data-aos="fade-up" data-aos-duration="2000">
    <h2>{{section.sub_text}}</h2>
    <h3>{{section.heading_text}}</h3>
    
    <div class="jobs">
        {% for job in section.jobs %}
            <div class="job">
                <a href="{{ROOT}}services">
                    <img src="{{job.icon}}" class="icon" alt="{{job.icon-alt}}">
                    <h4>{{job.title}}</h4>
                    {% for paragraph in job.description %}
                    <p>{{paragraph}}</p>
                    {% endfor %}
                </a>
            </div>
        {% endfor %}
    </div>

</section>